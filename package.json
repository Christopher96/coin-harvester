{
  "name": "coin-harvester",
  "version": "0.1.0",
  "dependencies": {
    "cross-env": "^5.1.5",
    "express": "^4.17.1",
    "mongoose": "5.0.12",
    "morgan": "^1.9.0",
    "node-fetch": "^2.6.0",
    "nodemon": "^1.11.0"
  },
  "scripts": {
    "server": "nodemon server --watch server",
    "prod": "cross-env NODE_ENV=production nodemon server --watch server"
  }
}
